use crate::data::{Args, Data, Frequency, State};

/// A `struct` that holds the data for the solutions generated from the given anagram.
pub struct Solution {

}

impl From<State> for Solution {
    fn from(state: State) -> Self {
        todo!()
    }
}